<div class="row text-center">
	<div class="col-md-8 offset-md-2">
     <% breadcrumb :page, @page %>
        <%= image_tag @page.image, :class=> 'img-responsive img-thumbnail' %>
		<h1><%= @page.title %></h1>
		<p><%= simple_format(@page.body) %></p>
	</div>
</div>